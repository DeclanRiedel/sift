[package]
name = "sift-website"
version.workspace = true
edition = "2024"
rust-version = "1.95"
license.workspace = true
publish = false

[dependencies]
tokio.workspace = true
topcoat = { version = "=0.6.2", default-features = false, features = ["router", "serve", "discover"] }

[build-dependencies]
brotli = "8"
flate2 = "1"
sha2 = "0.10"
