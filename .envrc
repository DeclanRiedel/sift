use flake
# Load repo-local env after the flake shell so .env can override flake
# defaults (e.g. live-test creds). See .env.example for the full var
# list; .env is gitignored.
dotenv_if_exists .env
